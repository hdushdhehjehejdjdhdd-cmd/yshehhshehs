-- RAYZ HUB - Vortexium Purple GUI
-- Cleaned & fixed version + purple Discord banner in Settings (smaller Join button)

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local SoundService = game:GetService("SoundService")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "VortexiumPurple"
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

-- Sounds
local hoverSound = Instance.new("Sound")
hoverSound.SoundId = "rbxassetid://4590657391"
hoverSound.Volume = 0.25
hoverSound.Parent = SoundService

local clickSound = Instance.new("Sound")
clickSound.SoundId = "rbxassetid://4590657391"
clickSound.Volume = 0.45
clickSound.Parent = SoundService

-- Main frame
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 520, 0, 340)
mainFrame.Position = UDim2.new(0.5, -260, 0.5, -170)
mainFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 28)
mainFrame.BorderSizePixel = 0
mainFrame.ClipsDescendants = true
mainFrame.Parent = screenGui

Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 12)

local outerGlow = Instance.new("UIStroke", mainFrame)
outerGlow.Color = Color3.fromRGB(190, 120, 255)
outerGlow.Thickness = 3
outerGlow.Transparency = 0.5
outerGlow.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

TweenService:Create(outerGlow, TweenInfo.new(3.2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
    Transparency = 0.28
}):Play()

-- Top bar
local topBar = Instance.new("Frame", mainFrame)
topBar.Size = UDim2.new(1,0,0,38)
topBar.BackgroundColor3 = Color3.fromRGB(35,25,60)
topBar.BorderSizePixel = 0
Instance.new("UICorner", topBar).CornerRadius = UDim.new(0,12)

local icon = Instance.new("ImageLabel", topBar)
icon.Size = UDim2.new(0,28,0,28)
icon.Position = UDim2.new(0,12,0,5)
icon.BackgroundTransparency = 1
icon.Image = "rbxassetid://121054081718087"
icon.ImageColor3 = Color3.fromRGB(220,180,255)
icon.ResampleMode = Enum.ResamplerMode.Pixelated
Instance.new("UICorner", icon).CornerRadius = UDim.new(1,0)

local title = Instance.new("TextLabel", topBar)
title.Size = UDim2.new(0,300,1,0)
title.Position = UDim2.new(0,50,0,0)
title.BackgroundTransparency = 1
title.Font = Enum.Font.Code
title.Text = "RAYZ HUB | Visuals menu"
title.TextColor3 = Color3.fromRGB(210,140,255)
title.TextSize = 16
title.TextXAlignment = Enum.TextXAlignment.Left

local minBtn = Instance.new("TextButton", topBar)
minBtn.Size = UDim2.new(0,28,0,28)
minBtn.Position = UDim2.new(1,-68,0,5)
minBtn.BackgroundColor3 = Color3.fromRGB(100,60,160)
minBtn.Text = "−"
minBtn.TextColor3 = Color3.new(1,1,1)
minBtn.Font = Enum.Font.Code
minBtn.TextSize = 20
Instance.new("UICorner", minBtn).CornerRadius = UDim.new(0,6)

local closeBtn = Instance.new("TextButton", topBar)
closeBtn.Size = UDim2.new(0,28,0,28)
closeBtn.Position = UDim2.new(1,-36,0,5)
closeBtn.BackgroundColor3 = Color3.fromRGB(140,40,80)
closeBtn.Text = "×"
closeBtn.TextColor3 = Color3.new(1,1,1)
closeBtn.Font = Enum.Font.Code
closeBtn.TextSize = 18
Instance.new("UICorner", closeBtn).CornerRadius = UDim.new(0,6)

-- Sidebar (now scrollable)
local sidebar = Instance.new("ScrollingFrame", mainFrame)
sidebar.Size = UDim2.new(0,140,1,-38)
sidebar.Position = UDim2.new(0,0,0,38)
sidebar.BackgroundColor3 = Color3.fromRGB(25,20,45)
sidebar.BorderSizePixel = 0
sidebar.ScrollBarThickness = 4
sidebar.ScrollBarImageColor3 = Color3.fromRGB(140,100,200)
sidebar.ScrollingDirection = Enum.ScrollingDirection.Y
sidebar.AutomaticCanvasSize = Enum.AutomaticSize.Y
sidebar.CanvasSize = UDim2.new(0,0,0,0)

-- Content area
local content = Instance.new("Frame", mainFrame)
content.Size = UDim2.new(1,-140,1,-38)
content.Position = UDim2.new(0,140,0,38)
content.BackgroundColor3 = Color3.fromRGB(18,18,28)
content.BorderSizePixel = 0

local activeTabLabel = Instance.new("TextLabel", content)
activeTabLabel.Size = UDim2.new(1,-20,0,30)
activeTabLabel.Position = UDim2.new(0,10,0,8)
activeTabLabel.BackgroundTransparency = 1
activeTabLabel.Font = Enum.Font.Code
activeTabLabel.Text = "Active Tab: PS99"
activeTabLabel.TextColor3 = Color3.fromRGB(190,140,255)
activeTabLabel.TextSize = 15
activeTabLabel.TextXAlignment = Enum.TextXAlignment.Left

local scroll = Instance.new("ScrollingFrame", content)
scroll.Size = UDim2.new(1,-20,1,-50)
scroll.Position = UDim2.new(0,10,0,45)
scroll.BackgroundTransparency = 1
scroll.BorderSizePixel = 0
scroll.ScrollBarThickness = 6
scroll.ScrollBarImageColor3 = Color3.fromRGB(140,100,200)
scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
scroll.CanvasSize = UDim2.new(0,0,0,0)
scroll.ScrollingDirection = Enum.ScrollingDirection.Y

local list = Instance.new("UIListLayout", scroll)
list.Padding = UDim.new(0,14)
list.HorizontalAlignment = Enum.HorizontalAlignment.Center
list.SortOrder = Enum.SortOrder.LayoutOrder

-- Tabs
local tabs = {"PS99", "ETFB", "PVB", "BF", "MM2", "ADM", "CREDITS"}
local selectedTab = "PS99"
local tabButtons = {}
local isMinimized = false

-- Game ID checks
local ps99Places = {8737899170, 16498369169, 16419852518, 17503543197, 140403681187145, 15502339080}
local isPS99 = table.find(ps99Places, game.PlaceId) ~= nil

local etfbPlaces = {131623223084840}
local isETFB = table.find(etfbPlaces, game.PlaceId) ~= nil

local pvbPlaces = {127742093697776}  -- Plants Vs Brainrots
local isPVB = table.find(pvbPlaces, game.PlaceId) ~= nil

local bfPlaces = {2753915549, 4442272183, 7449423635}  -- Blox Fruits (Main, Second Sea, Third Sea)
local isBF = table.find(bfPlaces, game.PlaceId) ~= nil

local mm2Places = {142823291, 9551640993}
local isMM2 = table.find(mm2Places, game.PlaceId) ~= nil

local admPlaces = {920587237}
local isADM = table.find(admPlaces, game.PlaceId) ~= nil

local normalSize = UDim2.new(0,520,0,340)
local minimizedSize = UDim2.new(0,340,0,38)

-- Create tab buttons
for i, name in ipairs(tabs) do
    local btn = Instance.new("TextButton")
    btn.Name = name
    btn.Size = UDim2.new(1,-12,0,42)
    btn.Position = UDim2.new(0,6,0,8+(i-1)*48)
    btn.BackgroundColor3 = Color3.fromRGB(35,25,60)
    btn.BorderSizePixel = 0
    btn.Font = Enum.Font.Code
    btn.Text = name
    btn.TextColor3 = Color3.fromRGB(200,160,255)
    btn.TextSize = 15
    btn.AutoButtonColor = false
    btn.Parent = sidebar

    Instance.new("UICorner", btn).CornerRadius = UDim.new(0,8)

    local left = Instance.new("Frame", btn)
    left.Size = UDim2.new(0,0,1,0)
    left.Position = UDim2.new(0,-2,0,0)
    left.BackgroundColor3 = Color3.fromRGB(200,110,255)
    left.BorderSizePixel = 0
    Instance.new("UICorner", left).CornerRadius = UDim.new(1,0)

    local glow = Instance.new("UIStroke", btn)
    glow.Color = Color3.fromRGB(210,130,255)
    glow.Thickness = 2.5
    glow.Transparency = 0.7
    glow.Enabled = false

    Instance.new("UIGradient", glow).Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Color3.fromRGB(255,220,255)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(140,80,255))
    }

    tabButtons[name] = {btn=btn, left=left, glow=glow, pulse=nil}

    btn.MouseEnter:Connect(function()
        if isMinimized or selectedTab == name then return end
        pcall(function() hoverSound:Play() end)
        TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3=Color3.fromRGB(55,40,95), Size=UDim2.new(1,-6,0,44)}):Play()
        TweenService:Create(glow, TweenInfo.new(0.2), {Transparency=0.35, Enabled=true}):Play()
    end)

    btn.MouseLeave:Connect(function()
        if isMinimized or selectedTab == name then return end
        TweenService:Create(btn, TweenInfo.new(0.24), {BackgroundColor3=Color3.fromRGB(35,25,60), Size=UDim2.new(1,-12,0,42)}):Play()
        TweenService:Create(glow, TweenInfo.new(0.24), {Transparency=0.7}):Play()
    end)

    btn.MouseButton1Click:Connect(function()
        if isMinimized or selectedTab == name then return end
        pcall(function() clickSound:Play() end)

        local old = tabButtons[selectedTab]
        if old then
            TweenService:Create(old.btn, TweenInfo.new(0.32, Enum.EasingStyle.Quint), {BackgroundColor3=Color3.fromRGB(35,25,60), TextColor3=Color3.fromRGB(200,160,255)}):Play()
            TweenService:Create(old.left, TweenInfo.new(0.28, Enum.EasingStyle.Back), {Size=UDim2.new(0,0,1,0)}):Play()
            TweenService:Create(old.glow, TweenInfo.new(0.32), {Transparency=0.7}):Play()
            if old.pulse then old.pulse:Cancel() end
        end

        selectedTab = name

        TweenService:Create(btn, TweenInfo.new(0.32, Enum.EasingStyle.Quint), {BackgroundColor3=Color3.fromRGB(70,50,115), TextColor3=Color3.fromRGB(240,210,255)}):Play()
        TweenService:Create(left, TweenInfo.new(0.32, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size=UDim2.new(0,4,1,0)}):Play()
        TweenService:Create(glow, TweenInfo.new(0.32), {Transparency=0.4, Enabled=true}):Play()

        local p = TweenService:Create(glow, TweenInfo.new(2.1, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {Transparency=0.22})
        p:Play()
        tabButtons[name].pulse = p

        activeTabLabel.Text = "Active Tab: " .. name
        rebuildContent()
    end)

    if name == "PS99" then
        btn.BackgroundColor3 = Color3.fromRGB(70,50,115)
        btn.TextColor3 = Color3.fromRGB(240,210,255)
        left.Size = UDim2.new(0,4,1,0)
        glow.Transparency = 0.4
        glow.Enabled = true
        local p = TweenService:Create(glow, TweenInfo.new(2.1, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {Transparency=0.22})
        p:Play()
        tabButtons[name].pulse = p
    end
end

-- (rebuildContent function and everything below remains exactly the same as in your previous version)

function rebuildContent()
    for _, v in scroll:GetChildren() do
        if v:IsA("GuiObject") and v ~= list then
            v:Destroy()
        end
    end

    task.wait()

    if selectedTab == "PS99" then
        if not isPS99 then
            local err = Instance.new("TextLabel", scroll)
            err.Size = UDim2.new(1,-40,0,80)
            err.BackgroundTransparency = 1
            err.Font = Enum.Font.Code
            err.Text = "This script won't work on this game.\n(Place ID not recognized as PS99)"
            err.TextColor3 = Color3.fromRGB(255,90,90)
            err.TextSize = 18
            err.TextWrapped = true
            err.TextYAlignment = Enum.TextYAlignment.Center
            return
        end

        local sections = {
            {name="Huge Hunter",   url="https://rayzhubb.vercel.app/visuals/ps99/hunter.lua"},
            {name="Pet Spawner",   url="https://rayzhubb.vercel.app/visuals/ps99/pets.lua"},
            {name="Gems Spawner",  url="https://rayzhubb.vercel.app/visuals/ps99/gems.lua"},
        }

        for _, s in sections do
            local card = Instance.new("Frame", scroll)
            card.Size = UDim2.new(1,-20,0,90)
            card.BackgroundColor3 = Color3.fromRGB(30,25,50)
            card.BorderSizePixel = 0
            Instance.new("UICorner", card).CornerRadius = UDim.new(0,10)

            local strk = Instance.new("UIStroke", card)
            strk.Color = Color3.fromRGB(160,100,220)
            strk.Thickness = 1.5
            strk.Transparency = 0.6

            local lbl = Instance.new("TextLabel", card)
            lbl.Size = UDim2.new(1,-20,0,30)
            lbl.Position = UDim2.new(0,10,0,8)
            lbl.BackgroundTransparency = 1
            lbl.Font = Enum.Font.Code
            lbl.Text = s.name
            lbl.TextColor3 = Color3.fromRGB(220,180,255)
            lbl.TextSize = 17
            lbl.TextXAlignment = Enum.TextXAlignment.Left

            local btn = Instance.new("TextButton", card)
            btn.Size = UDim2.new(0,140,0,38)
            btn.Position = UDim2.new(0.5,-70,1,-46)
            btn.BackgroundColor3 = Color3.fromRGB(90,50,160)
            btn.BorderSizePixel = 0
            btn.Font = Enum.Font.Code
            btn.Text = "Execute"
            btn.TextColor3 = Color3.new(1,1,1)
            btn.TextSize = 16
            Instance.new("UICorner", btn).CornerRadius = UDim.new(0,8)

            local btnstrk = Instance.new("UIStroke", btn)
            btnstrk.Color = Color3.fromRGB(200,140,255)
            btnstrk.Thickness = 1.8
            btnstrk.Transparency = 0.4

            btn.MouseEnter:Connect(function()
                TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3=Color3.fromRGB(120,70,200)}):Play()
                TweenService:Create(btnstrk, TweenInfo.new(0.2), {Transparency=0.2}):Play()
            end)

            btn.MouseLeave:Connect(function()
                TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3=Color3.fromRGB(90,50,160)}):Play()
                TweenService:Create(btnstrk, TweenInfo.new(0.2), {Transparency=0.4}):Play()
            end)

            btn.MouseButton1Click:Connect(function()
                pcall(clickSound.Play, clickSound)
                local ok, code = pcall(game.HttpGet, game, s.url, true)
                if not ok then warn("HttpGet failed: " .. tostring(code)) return end
                local fn; ok, fn = pcall(loadstring, code)
                if not ok then warn("loadstring failed: " .. tostring(fn)) return end
                ok, code = pcall(fn)
                if not ok then warn("Execution error: " .. tostring(code)) end
            end)
        end

    elseif selectedTab == "ETFB" then
        if not isETFB then
            local err = Instance.new("TextLabel", scroll)
            err.Size = UDim2.new(1,-40,0,80)
            err.BackgroundTransparency = 1
            err.Font = Enum.Font.Code
            err.Text = "This script won't work on this game.\n(Place ID not recognized as ETFB)"
            err.TextColor3 = Color3.fromRGB(255,90,90)
            err.TextSize = 18
            err.TextWrapped = true
            err.TextYAlignment = Enum.TextYAlignment.Center
            return
        end

        local sections = {
            {name = "DUPER", url = "https://rayzhubb.vercel.app/visuals/etfb/dupe.lua"},
        }

        for _, s in sections do
            local card = Instance.new("Frame", scroll)
            card.Size = UDim2.new(1,-20,0,90)
            card.BackgroundColor3 = Color3.fromRGB(30,25,50)
            card.BorderSizePixel = 0
            Instance.new("UICorner", card).CornerRadius = UDim.new(0,10)

            local strk = Instance.new("UIStroke", card)
            strk.Color = Color3.fromRGB(160,100,220)
            strk.Thickness = 1.5
            strk.Transparency = 0.6

            local lbl = Instance.new("TextLabel", card)
            lbl.Size = UDim2.new(1,-20,0,30)
            lbl.Position = UDim2.new(0,10,0,8)
            lbl.BackgroundTransparency = 1
            lbl.Font = Enum.Font.Code
            lbl.Text = s.name
            lbl.TextColor3 = Color3.fromRGB(220,180,255)
            lbl.TextSize = 17
            lbl.TextXAlignment = Enum.TextXAlignment.Left

            local btn = Instance.new("TextButton", card)
            btn.Size = UDim2.new(0,140,0,38)
            btn.Position = UDim2.new(0.5,-70,1,-46)
            btn.BackgroundColor3 = Color3.fromRGB(90,50,160)
            btn.BorderSizePixel = 0
            btn.Font = Enum.Font.Code
            btn.Text = "Execute"
            btn.TextColor3 = Color3.new(1,1,1)
            btn.TextSize = 16
            Instance.new("UICorner", btn).CornerRadius = UDim.new(0,8)

            local btnstrk = Instance.new("UIStroke", btn)
            btnstrk.Color = Color3.fromRGB(200,140,255)
            btnstrk.Thickness = 1.8
            btnstrk.Transparency = 0.4

            btn.MouseEnter:Connect(function()
                TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3=Color3.fromRGB(120,70,200)}):Play()
                TweenService:Create(btnstrk, TweenInfo.new(0.2), {Transparency=0.2}):Play()
            end)

            btn.MouseLeave:Connect(function()
                TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3=Color3.fromRGB(90,50,160)}):Play()
                TweenService:Create(btnstrk, TweenInfo.new(0.2), {Transparency=0.4}):Play()
            end)

            btn.MouseButton1Click:Connect(function()
                pcall(clickSound.Play, clickSound)
                local ok, code = pcall(game.HttpGet, game, s.url, true)
                if not ok then warn("HttpGet failed: " .. tostring(code)) return end
                local fn; ok, fn = pcall(loadstring, code)
                if not ok then warn("loadstring failed: " .. tostring(fn)) return end
                ok, code = pcall(fn)
                if not ok then warn("Execution error: " .. tostring(code)) end
            end)
        end

    elseif selectedTab == "PVB" then
        if not isPVB then
            local err = Instance.new("TextLabel", scroll)
            err.Size = UDim2.new(1,-40,0,80)
            err.BackgroundTransparency = 1
            err.Font = Enum.Font.Code
            err.Text = "This script won't work on this game.\n(Place ID not recognized as PVB)"
            err.TextColor3 = Color3.fromRGB(255,90,90)
            err.TextSize = 18
            err.TextWrapped = true
            err.TextYAlignment = Enum.TextYAlignment.Center
            return
        end

        local sections = {
            {name = "SPAWNER", url = "https://rayzhubb.vercel.app/visuals/pvb/spawner.lua"},
        }

        for _, s in sections do
            local card = Instance.new("Frame", scroll)
            card.Size = UDim2.new(1,-20,0,90)
            card.BackgroundColor3 = Color3.fromRGB(30,25,50)
            card.BorderSizePixel = 0
            Instance.new("UICorner", card).CornerRadius = UDim.new(0,10)

            local strk = Instance.new("UIStroke", card)
            strk.Color = Color3.fromRGB(160,100,220)
            strk.Thickness = 1.5
            strk.Transparency = 0.6

            local lbl = Instance.new("TextLabel", card)
            lbl.Size = UDim2.new(1,-20,0,30)
            lbl.Position = UDim2.new(0,10,0,8)
            lbl.BackgroundTransparency = 1
            lbl.Font = Enum.Font.Code
            lbl.Text = s.name
            lbl.TextColor3 = Color3.fromRGB(220,180,255)
            lbl.TextSize = 17
            lbl.TextXAlignment = Enum.TextXAlignment.Left

            local btn = Instance.new("TextButton", card)
            btn.Size = UDim2.new(0,140,0,38)
            btn.Position = UDim2.new(0.5,-70,1,-46)
            btn.BackgroundColor3 = Color3.fromRGB(90,50,160)
            btn.BorderSizePixel = 0
            btn.Font = Enum.Font.Code
            btn.Text = "Execute"
            btn.TextColor3 = Color3.new(1,1,1)
            btn.TextSize = 16
            Instance.new("UICorner", btn).CornerRadius = UDim.new(0,8)

            local btnstrk = Instance.new("UIStroke", btn)
            btnstrk.Color = Color3.fromRGB(200,140,255)
            btnstrk.Thickness = 1.8
            btnstrk.Transparency = 0.4

            btn.MouseEnter:Connect(function()
                TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3=Color3.fromRGB(120,70,200)}):Play()
                TweenService:Create(btnstrk, TweenInfo.new(0.2), {Transparency=0.2}):Play()
            end)

            btn.MouseLeave:Connect(function()
                TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3=Color3.fromRGB(90,50,160)}):Play()
                TweenService:Create(btnstrk, TweenInfo.new(0.2), {Transparency=0.4}):Play()
            end)

            btn.MouseButton1Click:Connect(function()
                pcall(clickSound.Play, clickSound)
                local ok, code = pcall(game.HttpGet, game, s.url, true)
                if not ok then warn("HttpGet failed: " .. tostring(code)) return end
                local fn; ok, fn = pcall(loadstring, code)
                if not ok then warn("loadstring failed: " .. tostring(fn)) return end
                ok, code = pcall(fn)
                if not ok then warn("Execution error: " .. tostring(code)) end
            end)
        end

    elseif selectedTab == "BF" then
        if not isBF then
            local err = Instance.new("TextLabel", scroll)
            err.Size = UDim2.new(1,-40,0,80)
            err.BackgroundTransparency = 1
            err.Font = Enum.Font.Code
            err.Text = "This script won't work on this game.\n(Place ID not recognized as Blox Fruits)"
            err.TextColor3 = Color3.fromRGB(255,90,90)
            err.TextSize = 18
            err.TextWrapped = true
            err.TextYAlignment = Enum.TextYAlignment.Center
            return
        end

        local sections = {
            {name = "Trade Freezer", url = "https://rayzhubb.vercel.app/visuals/bf/trade.lua"},
        }

        for _, s in sections do
            local card = Instance.new("Frame", scroll)
            card.Size = UDim2.new(1,-20,0,90)
            card.BackgroundColor3 = Color3.fromRGB(30,25,50)
            card.BorderSizePixel = 0
            Instance.new("UICorner", card).CornerRadius = UDim.new(0,10)

            local strk = Instance.new("UIStroke", card)
            strk.Color = Color3.fromRGB(160,100,220)
            strk.Thickness = 1.5
            strk.Transparency = 0.6

            local lbl = Instance.new("TextLabel", card)
            lbl.Size = UDim2.new(1,-20,0,30)
            lbl.Position = UDim2.new(0,10,0,8)
            lbl.BackgroundTransparency = 1
            lbl.Font = Enum.Font.Code
            lbl.Text = s.name
            lbl.TextColor3 = Color3.fromRGB(220,180,255)
            lbl.TextSize = 17
            lbl.TextXAlignment = Enum.TextXAlignment.Left

            local btn = Instance.new("TextButton", card)
            btn.Size = UDim2.new(0,140,0,38)
            btn.Position = UDim2.new(0.5,-70,1,-46)
            btn.BackgroundColor3 = Color3.fromRGB(90,50,160)
            btn.BorderSizePixel = 0
            btn.Font = Enum.Font.Code
            btn.Text = "Execute"
            btn.TextColor3 = Color3.new(1,1,1)
            btn.TextSize = 16
            Instance.new("UICorner", btn).CornerRadius = UDim.new(0,8)

            local btnstrk = Instance.new("UIStroke", btn)
            btnstrk.Color = Color3.fromRGB(200,140,255)
            btnstrk.Thickness = 1.8
            btnstrk.Transparency = 0.4

            btn.MouseEnter:Connect(function()
                TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3=Color3.fromRGB(120,70,200)}):Play()
                TweenService:Create(btnstrk, TweenInfo.new(0.2), {Transparency=0.2}):Play()
            end)

            btn.MouseLeave:Connect(function()
                TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3=Color3.fromRGB(90,50,160)}):Play()
                TweenService:Create(btnstrk, TweenInfo.new(0.2), {Transparency=0.4}):Play()
            end)

            btn.MouseButton1Click:Connect(function()
                pcall(clickSound.Play, clickSound)
                local ok, code = pcall(game.HttpGet, game, s.url, true)
                if not ok then warn("HttpGet failed: " .. tostring(code)) return end
                local fn; ok, fn = pcall(loadstring, code)
                if not ok then warn("loadstring failed: " .. tostring(fn)) return end
                ok, code = pcall(fn)
                if not ok then warn("Execution error: " .. tostring(code)) end
            end)
        end

    elseif selectedTab == "MM2" then
        if not isMM2 then
            local err = Instance.new("TextLabel", scroll)
            err.Size = UDim2.new(1,-40,0,80)
            err.BackgroundTransparency = 1
            err.Font = Enum.Font.Code
            err.Text = "This script won't work on this game.\n(Place ID not recognized as MM2)"
            err.TextColor3 = Color3.fromRGB(255,90,90)
            err.TextSize = 18
            err.TextWrapped = true
            err.TextYAlignment = Enum.TextYAlignment.Center
            return
        end

        local sections = {
            {name = "Items Spawner", url = "https://rayzhubb.vercel.app/visuals/mm2/spawner.lua"},
        }

        for _, s in sections do
            local card = Instance.new("Frame", scroll)
            card.Size = UDim2.new(1,-20,0,90)
            card.BackgroundColor3 = Color3.fromRGB(30,25,50)
            card.BorderSizePixel = 0
            Instance.new("UICorner", card).CornerRadius = UDim.new(0,10)

            local strk = Instance.new("UIStroke", card)
            strk.Color = Color3.fromRGB(160,100,220)
            strk.Thickness = 1.5
            strk.Transparency = 0.6

            local lbl = Instance.new("TextLabel", card)
            lbl.Size = UDim2.new(1,-20,0,30)
            lbl.Position = UDim2.new(0,10,0,8)
            lbl.BackgroundTransparency = 1
            lbl.Font = Enum.Font.Code
            lbl.Text = s.name
            lbl.TextColor3 = Color3.fromRGB(220,180,255)
            lbl.TextSize = 17
            lbl.TextXAlignment = Enum.TextXAlignment.Left

            local btn = Instance.new("TextButton", card)
            btn.Size = UDim2.new(0,140,0,38)
            btn.Position = UDim2.new(0.5,-70,1,-46)
            btn.BackgroundColor3 = Color3.fromRGB(90,50,160)
            btn.BorderSizePixel = 0
            btn.Font = Enum.Font.Code
            btn.Text = "Execute"
            btn.TextColor3 = Color3.new(1,1,1)
            btn.TextSize = 16
            Instance.new("UICorner", btn).CornerRadius = UDim.new(0,8)

            local btnstrk = Instance.new("UIStroke", btn)
            btnstrk.Color = Color3.fromRGB(200,140,255)
            btnstrk.Thickness = 1.8
            btnstrk.Transparency = 0.4

            btn.MouseEnter:Connect(function()
                TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3=Color3.fromRGB(120,70,200)}):Play()
                TweenService:Create(btnstrk, TweenInfo.new(0.2), {Transparency=0.2}):Play()
            end)

            btn.MouseLeave:Connect(function()
                TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3=Color3.fromRGB(90,50,160)}):Play()
                TweenService:Create(btnstrk, TweenInfo.new(0.2), {Transparency=0.4}):Play()
            end)

            btn.MouseButton1Click:Connect(function()
                pcall(clickSound.Play, clickSound)
                local ok, code = pcall(game.HttpGet, game, s.url, true)
                if not ok then warn("HttpGet failed: " .. tostring(code)) return end
                local fn; ok, fn = pcall(loadstring, code)
                if not ok then warn("loadstring failed: " .. tostring(fn)) return end
                ok, code = pcall(fn)
                if not ok then warn("Execution error: " .. tostring(code)) end
            end)
        end

    elseif selectedTab == "ADM" then
        if not isADM then
            local err = Instance.new("TextLabel", scroll)
            err.Size = UDim2.new(1,-40,0,80)
            err.BackgroundTransparency = 1
            err.Font = Enum.Font.Code
            err.Text = "This script won't work on this game.\n(Place ID not recognized as ADM place)"
            err.TextColor3 = Color3.fromRGB(255,90,90)
            err.TextSize = 18
            err.TextWrapped = true
            err.TextYAlignment = Enum.TextYAlignment.Center
            return
        end

        local sections = {
            {name = "Pets Spawner", url = "https://rayzhubb.vercel.app/visuals/adm/spawner.lua"},
        }

        for _, s in sections do
            local card = Instance.new("Frame", scroll)
            card.Size = UDim2.new(1,-20,0,90)
            card.BackgroundColor3 = Color3.fromRGB(30,25,50)
            card.BorderSizePixel = 0
            Instance.new("UICorner", card).CornerRadius = UDim.new(0,10)

            local strk = Instance.new("UIStroke", card)
            strk.Color = Color3.fromRGB(160,100,220)
            strk.Thickness = 1.5
            strk.Transparency = 0.6

            local lbl = Instance.new("TextLabel", card)
            lbl.Size = UDim2.new(1,-20,0,30)
            lbl.Position = UDim2.new(0,10,0,8)
            lbl.BackgroundTransparency = 1
            lbl.Font = Enum.Font.Code
            lbl.Text = s.name
            lbl.TextColor3 = Color3.fromRGB(220,180,255)
            lbl.TextSize = 17
            lbl.TextXAlignment = Enum.TextXAlignment.Left

            local btn = Instance.new("TextButton", card)
            btn.Size = UDim2.new(0,140,0,38)
            btn.Position = UDim2.new(0.5,-70,1,-46)
            btn.BackgroundColor3 = Color3.fromRGB(90,50,160)
            btn.BorderSizePixel = 0
            btn.Font = Enum.Font.Code
            btn.Text = "Execute"
            btn.TextColor3 = Color3.new(1,1,1)
            btn.TextSize = 16
            Instance.new("UICorner", btn).CornerRadius = UDim.new(0,8)

            local btnstrk = Instance.new("UIStroke", btn)
            btnstrk.Color = Color3.fromRGB(200,140,255)
            btnstrk.Thickness = 1.8
            btnstrk.Transparency = 0.4

            btn.MouseEnter:Connect(function()
                TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3=Color3.fromRGB(120,70,200)}):Play()
                TweenService:Create(btnstrk, TweenInfo.new(0.2), {Transparency=0.2}):Play()
            end)

            btn.MouseLeave:Connect(function()
                TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3=Color3.fromRGB(90,50,160)}):Play()
                TweenService:Create(btnstrk, TweenInfo.new(0.2), {Transparency=0.4}):Play()
            end)

            btn.MouseButton1Click:Connect(function()
                pcall(clickSound.Play, clickSound)
                local ok, code = pcall(game.HttpGet, game, s.url, true)
                if not ok then warn("HttpGet failed: " .. tostring(code)) return end
                local fn; ok, fn = pcall(loadstring, code)
                if not ok then warn("loadstring failed: " .. tostring(fn)) return end
                ok, code = pcall(fn)
                if not ok then warn("Execution error: " .. tostring(code)) end
            end)
        end

    elseif selectedTab == "CREDITS" then
        -- Discord invite banner (smaller Join button)
        local banner = Instance.new("Frame", scroll)
        banner.Size = UDim2.new(1, -20, 0, 110)
        banner.BackgroundColor3 = Color3.fromRGB(45, 35, 75)
        banner.BorderSizePixel = 0
        banner.LayoutOrder = 1

        local corner = Instance.new("UICorner", banner)
        corner.CornerRadius = UDim.new(0, 12)

        local stroke = Instance.new("UIStroke", banner)
        stroke.Color = Color3.fromRGB(170, 120, 230)
        stroke.Thickness = 2
        stroke.Transparency = 0.4

        -- Avatar / icon left
        local avatar = Instance.new("ImageLabel", banner)
        avatar.Size = UDim2.new(0, 72, 0, 72)
        avatar.Position = UDim2.new(0, 16, 0.5, -53)
        avatar.BackgroundTransparency = 1
        avatar.Image = "rbxassetid://121054081718087"
        avatar.ResampleMode = Enum.ResamplerMode.Pixelated

        local avCorner = Instance.new("UICorner", avatar)
        avCorner.CornerRadius = UDim.new(0, 5)

        -- Main text (bold title)
        local serverName = Instance.new("TextLabel", banner)
        serverName.Size = UDim2.new(1, -110, 0, 32)
        serverName.Position = UDim2.new(0, 100, 0, 14)
        serverName.BackgroundTransparency = 1
        serverName.Font = Enum.Font.Code
        serverName.Text = "RAYZ HUB"
        serverName.TextColor3 = Color3.fromRGB(235, 210, 255)
        serverName.TextSize = 22
        serverName.TextXAlignment = Enum.TextXAlignment.Left
        serverName.TextYAlignment = Enum.TextYAlignment.Center

        -- Subtitle / invite link
        local inviteText = Instance.new("TextLabel", banner)
        inviteText.Size = UDim2.new(1, -110, 0, 24)
        inviteText.Position = UDim2.new(0, 100, 0, 46)
        inviteText.BackgroundTransparency = 1
        inviteText.Font = Enum.Font.Code
        inviteText.Text = "discord.gg/rayzhub"
        inviteText.TextColor3 = Color3.fromRGB(190, 170, 240)
        inviteText.TextSize = 16
        inviteText.TextXAlignment = Enum.TextXAlignment.Left

        -- Smaller Join button
        local joinBtn = Instance.new("TextButton", banner)
        joinBtn.Size = UDim2.new(1, -32, 0, 26)
        joinBtn.Position = UDim2.new(0, 16, 1, -32)
        joinBtn.BackgroundColor3 = Color3.fromRGB(60, 200, 100)
        joinBtn.BorderSizePixel = 0
        joinBtn.Font = Enum.Font.Code
        joinBtn.Text = "Join"
        joinBtn.TextColor3 = Color3.new(1,1,1)
        joinBtn.TextSize = 18

        local btnCorner = Instance.new("UICorner", joinBtn)
        btnCorner.CornerRadius = UDim.new(0, 10)

        local btnStroke = Instance.new("UIStroke", joinBtn)
        btnStroke.Color = Color3.fromRGB(140, 255, 180)
        btnStroke.Thickness = 2
        btnStroke.Transparency = 0.3

        joinBtn.MouseEnter:Connect(function()
            TweenService:Create(joinBtn, TweenInfo.new(0.18), {BackgroundColor3 = Color3.fromRGB(80, 220, 120)}):Play()
            TweenService:Create(btnStroke, TweenInfo.new(0.18), {Transparency = 0.1}):Play()
        end)

        joinBtn.MouseLeave:Connect(function()
            TweenService:Create(joinBtn, TweenInfo.new(0.18), {BackgroundColor3 = Color3.fromRGB(60, 200, 100)}):Play()
            TweenService:Create(btnStroke, TweenInfo.new(0.18), {Transparency = 0.3}):Play()
        end)

        joinBtn.MouseButton1Click:Connect(function()
            pcall(clickSound.Play, clickSound)
            setclipboard("https://discord.gg/rayzhub")
        end)

    else
        local ph = Instance.new("TextLabel", scroll)
        ph.Size = UDim2.new(1,0,0,100)
        ph.BackgroundTransparency = 1
        ph.Font = Enum.Font.Code
        ph.Text = selectedTab .. " tab\ncoming soon..."
        ph.TextColor3 = Color3.fromRGB(160,160,180)
        ph.TextSize = 20
        ph.TextWrapped = true
    end

    scroll.CanvasPosition = Vector2.zero
end

-- Minimize toggle
local function toggleMinimize()
    isMinimized = not isMinimized
    if isMinimized then
        TweenService:Create(mainFrame, TweenInfo.new(0.42, Enum.EasingStyle.Back, Enum.EasingDirection.In), {Size = minimizedSize}):Play()
        TweenService:Create(sidebar, TweenInfo.new(0.3), {BackgroundTransparency=1}):Play()
        TweenService:Create(content, TweenInfo.new(0.3), {BackgroundTransparency=1}):Play()
        minBtn.Text = "+"
    else
        TweenService:Create(mainFrame, TweenInfo.new(0.42, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = normalSize}):Play()
        TweenService:Create(sidebar, TweenInfo.new(0.3), {BackgroundTransparency=0}):Play()
        TweenService:Create(content, TweenInfo.new(0.3), {BackgroundTransparency=0}):Play()
        minBtn.Text = "−"
    end
end
minBtn.MouseButton1Click:Connect(toggleMinimize)

-- Close
closeBtn.MouseButton1Click:Connect(function()
    TweenService:Create(mainFrame, TweenInfo.new(0.38, Enum.EasingStyle.Back, Enum.EasingDirection.In), {
        BackgroundTransparency = 1,
        Size = UDim2.new(0,420,0,260)
    }):Play()
    task.wait(0.4)
    screenGui:Destroy()
end)

-- Dragging
local dragging, dragInput, dragStart, startPos

local function updateDrag(input)
    local delta = input.Position - dragStart
    mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end

topBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = mainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then dragging = false end
        end)
    end
end)

topBar.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then updateDrag(input) end
end)

-- Initial open animation
mainFrame.BackgroundTransparency = 1
mainFrame.Size = UDim2.new(0,480,0,300)
TweenService:Create(mainFrame, TweenInfo.new(0.65, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
    BackgroundTransparency = 0,
    Size = normalSize
}):Play()

-- Insert key toggle
UserInputService.InputBegan:Connect(function(input, gpe)
    if gpe then return end
    if input.KeyCode == Enum.KeyCode.Insert then
        screenGui.Enabled = not screenGui.Enabled
    end
end)

-- Load default tab
rebuildContent()

print("RAYZ HUB loaded")